
# dos2unix --text file format converter 
   dos2unix  <file>
   convert a file from unix text file to DOS file format

# unix2dos --text file format coverter 
  just opposite of dos2unix
  
# dd --convert and copy a file
  sudo dd if=/dev/sda of=/dev/sdb
  backup an entire hard disk to a secondary drive
  sudo dd is=/dev/cdrom of=cdrom.iso bs=2048
  create a CDROM backup to an .ISO
#bzip2 --block-sorting file compressor
  bzip2 -z <filename>
  [-z]compress a file
  bzip2 -d <filename> 
  [-d]decompress a file
  

#gzip --compress or expand files
  gzip -d <file> 
  [-d] decompress a file
  [-r] recursive
  

#tar --an archiving utility
  tar -cvf archive.tar
  [-c]create archive [-v]verbose[-f]specify file name type
  
  tar -xvf <file.tar>
  [-x]extract [-v]verbose [-f] specify the file nametype
  extract a tar file
  
#unzip--extract compressed zip files
   unzip <file>
#zip--package and compress files
   zip resolve.zip /etc/resolv.conf
   it compress the resolve.conf file into zip archive
   
#base64 --base64 encode/decode tool
   base64 <file> 
   encode the contents of a file to base64
   echo 'happy halloween' | base64
   base64 encode the string
   base64 -d halloween.txt
   [-d] decode
   decode a base file
#md5sum --MD5 message digest utility 
   md5sum <file> compute MD5 checksum of a file
   md5sum -c <file.md5>
   [-c]check or verify
   verify MD5 checksum of a file
#sha1sum --SHA1 message digest utility
   sha1sum -c <file.sha1>
   [-c] check/verify
   verify SHA1 checksum for a file
#xxd -- make a hex dump or convert hex (non-root)
  xxd <file> 
  create a hex dump of a file 
#man --system reference manual

  man man.7 open the man.7 manual page
#apropos --search the manual page content
 apropos metasploit 
 search the man pages for the keyword metasploit

#find --search for a fies in a directory hierarchy
find / -name*.jpg
   find every jpg file
#Locate --find files by name
  locate nc.exe
#which locate a command
  which msfconsole
  locate msfconsole binary
#id --loacet userid or group
#whoami--print effective user id
#w -- displays who is logged on
#hostname-- show or set system host name
sudo hostname 
set a new hostname 

#arch --print machine architecture
 arch  display machine architecture 

#uname -- print system information
 uname -a 
 [-a] prints all system information

#df -- report file system disk space usage 
 df -ah / 
 [-a] all information
 [-h] human readable format
#du --estimate file space usage
 du -h 
 [-h] human readable format
du -ah /home
 [-a] write counts for all files 
 [-h] human readable format
#fdisk -- manipulate disk partition table
 sudo fdisk -l
 [-l] list partitions and exit
 
 sudo fdisk /dev/sda
  view and manage disk partitions of device /dev/sda
 #ps -- view snapshot of current processes
  ps aux
  [a] all processes [u]display user list
  [x]show non-attached process
  
#systemctl--system service manager
 sudo systemctl start apache2
 start apache web server 
 
 sudo systemctl stop apache2
 stop apache webserver
 sudo systemctl enable ssh
 enable ssh servers on startup
#top --display linux processes
 top 
 display an active linux list of processes
#pidof--find process id of running processes

 pidof bash
 show the process id of bash
#kill--send a signal to a process 
 kill -9 <process ID>
 send a SIGKILL signal to a process to shutdown process immidiately
#watch -n 5 date
  run the data  command every 5 seconds and watch output 
#stat -- display file or file system status 
  stat <file> Print in-depth file information
  
#Isof --list open files 
  Isof List open files
#Ipsci --list all PCI device
 Ipsci List all PCI device
 
#Isusb -- list USB devices 
Isusb 
list attached USB devices

#mount --mount a file system

 sudo mount <device_name> /media 
 Mount a device to the media folder
 
#unmount --unmount file system specified 

 sudo unmount <device name> OR <mount_point>
 unmount the file system specified

#adduser -- add a user to the system
 sudo adduser testuser
 Add the user "testuser" to the system 
 
#deluser -- delete a user from the system

#passwd -- change the user password 
 passwd kali
 change the password for the user kali
 sudo passwd -Sa
 [-S] status [-a]
#sudo --execute a command as another user
 sudo ifconfig
 Display network interface configuration
 sudo -I 
  List the allowed sudo commands
 sudo su 
  Switch to the root users
  
#su --substitute or switch user
 su kali 
 switch to kali user from root

#reboot --halt,power off , or reboot the machine
 su reboot now 
 reboot system immediately
#shutdown --halt ,poweroff ,or reboot the machine 
 sudo shutdown -r now [-r] : reboot
 sudo shutdown now 
 power off system immediately 
#chmod --change file mods bits
 chmod +x <file> grant execute permission on file 
 chmod -x <file> remove execute permission on file
